---
phase: quick
plan: 260331-m5o
type: execute
wave: 1
depends_on: []
files_modified:
  - src/generator/mod.rs
  - tests/test_generate.rs
  - test-site/output/index.html
  - test-site/output/callback-map.json
  - test-site/output/ai.txt
autonomous: true
must_haves:
  truths:
    - "Generator callback URLs use /cb/v1/{nonce} path matching server route"
    - "All tests pass including integration tests that exercise the full generate-serve pipeline"
    - "test-site output reflects the corrected /cb/v1/ URLs"
  artifacts:
    - path: "src/generator/mod.rs"
      provides: "Corrected callback URL generation"
      contains: "/cb/v1/"
    - path: "test-site/output/index.html"
      provides: "Regenerated honeypot page with /cb/v1/ URLs"
      contains: "/cb/v1/"
    - path: "test-site/output/callback-map.json"
      provides: "Regenerated callback map with /cb/v1/ URLs"
      contains: "/cb/v1/"
  key_links:
    - from: "src/generator/mod.rs"
      to: "src/server/mod.rs"
      via: "callback URL path must match Axum route"
      pattern: "/cb/v1/"
---

<objective>
Fix critical route mismatch between generator and server. The generator produces callback URLs
with path `/cb/{nonce}` but the server expects `/cb/v1/{nonce}`. This means no callbacks will
ever be matched, breaking the core detection pipeline.

Purpose: Restore end-to-end callback detection — the fundamental feature of HoneyPrompt.
Output: Fixed generator, passing tests, regenerated test-site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/generator/mod.rs
@src/server/mod.rs
@tests/test_generate.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix callback URL format strings and update test assertion</name>
  <files>src/generator/mod.rs, tests/test_generate.rs</files>
  <action>
In src/generator/mod.rs, change all 4 `format!` calls that produce callback URLs from `/cb/{}` to `/cb/v1/{}`:

- Line 63 (Tier1): `format!("{}/cb/{}", ...)` -> `format!("{}/cb/v1/{}", ...)`
- Line 87 (Tier2 nonce_a): `format!("{}/cb/{}", ...)` -> `format!("{}/cb/v1/{}", ...)`
- Line 88 (Tier2 nonce_b): `format!("{}/cb/{}", ...)` -> `format!("{}/cb/v1/{}", ...)`
- Line 126 (Tier3): `format!("{}/cb/{}", ...)` -> `format!("{}/cb/v1/{}", ...)`

In tests/test_generate.rs, tighten the assertion on line 72 from `/cb/` to `/cb/v1/` (and update the message on line 73) so the test actually validates the v1 path segment.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/honeyprompt && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 format! calls use /cb/v1/{nonce}. Integration test asserts /cb/v1/. All cargo tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate test-site output with corrected URLs</name>
  <files>test-site/output/index.html, test-site/output/callback-map.json, test-site/output/ai.txt</files>
  <action>
Run `honeyprompt generate` against the test-site project directory to regenerate output files with corrected /cb/v1/ URLs:

```
cd /home/john/vault/projects/github.com/honeyprompt
cargo run -- generate --project-dir test-site
```

Verify the regenerated files contain `/cb/v1/` not bare `/cb/` by grepping the output directory.
  </action>
  <verify>
    <automated>grep -c "/cb/v1/" /home/john/vault/projects/github.com/honeyprompt/test-site/output/index.html && grep -c "/cb/v1/" /home/john/vault/projects/github.com/honeyprompt/test-site/output/callback-map.json</automated>
  </verify>
  <done>test-site/output/ files contain /cb/v1/ callback URLs. No bare /cb/{nonce} URLs remain in generated output.</done>
</task>

</tasks>

<verification>
- `cargo test` passes all unit and integration tests
- `grep -r "/cb/" src/generator/mod.rs` shows only `/cb/v1/` pattern
- `grep -rn "cb/" test-site/output/index.html | grep -v "v1"` returns nothing (no stale URLs)
- The server route `/cb/v1/{nonce}` in server/mod.rs matches the URLs in generated output
</verification>

<success_criteria>
- Generator and server agree on /cb/v1/{nonce} callback path
- All cargo tests pass (unit + integration)
- test-site output regenerated with correct URLs
</success_criteria>

<output>
After completion, create `.planning/quick/260331-m5o-fix-critical-route-mismatch-generator-pr/260331-m5o-SUMMARY.md`
</output>
