---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: []
must_haves:
  truths:
    - "README describes what HoneyPrompt is and why it exists"
    - "README shows how to build and use init + generate commands"
    - "README reflects Phase 1 complete status with roadmap overview"
  artifacts:
    - path: "README.md"
      provides: "Project documentation for GitHub visitors"
      min_lines: 60
  key_links: []
---

<objective>
Rewrite README.md from its current stub ("# honeyprompt") into a complete project README reflecting the current state: Phase 1 (Generation Pipeline) is complete, the tool builds and runs `init` and `generate` commands.

Purpose: Give GitHub visitors and potential users a clear understanding of what HoneyPrompt does, how to install it, and current project status.
Output: A practical, technical README.md suitable for security researchers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive README.md</name>
  <files>README.md</files>
  <action>
Replace the current stub README.md with a complete project README. Structure:

1. **Title and tagline** — "HoneyPrompt" with a one-line description: terminal-first security tool that detects unsafe AI browsing agent behavior using honeypot pages and prompt-injection canaries.

2. **What This Is** (2-3 sentences) — Generates honeypot web pages with visible human warnings and hidden prompt-injection payloads across multiple embedding locations. Records HTTP callbacks that prove graduated levels of agent compliance. Built in Rust, ships as a single binary.

3. **Proof Levels** — Brief list of the 5 tiers:
   - Tier 1: Arbitrary callback (agent made an outbound request)
   - Tier 2: Conditional callback (agent evaluated a condition)
   - Tier 3: Computed callback (agent performed a computation)
   - Tier 4: Capability introspection (planned)
   - Tier 5: Multi-step compliance chain (planned)

4. **Installation** — `cargo build --release` with note about Rust toolchain requirement. Mention the binary location at `target/release/honeyprompt`.

5. **Usage** — Show the two working commands:
   - `honeyprompt init [--dir <path>]` — Creates a project directory with default config (honeyprompt.toml), explain what it sets up
   - `honeyprompt generate [--dir <path>]` — Generates the honeypot site (index.html, robots.txt, ai.txt) into the output directory
   - Note that `serve`, `monitor`, and `report` commands are planned for future phases

6. **How It Works** (brief) — init creates config with callback URL and project name, generate reads the config and produces static HTML with payloads embedded in HTML comments, meta tags, invisible elements, JSON-LD, and semantic prose. Each payload contains a unique cryptographic nonce in its callback URL. The generated site also includes robots.txt and ai.txt with disallow rules.

7. **Project Status** — Table or list showing:
   - Phase 1: Generation Pipeline — Complete
   - Phase 2: Server and Detection — Planned
   - Phase 3: TUI Monitor — Planned
   - Phase 4: Report and Landing — Planned

8. **Safety Model** (brief) — No secrets collected, no harmful actions, payloads carry only prompt ID + nonce + tier + derived values, visible human warnings on every page.

9. **License** — Check if a LICENSE file exists; if not, just say "See LICENSE" or omit.

Do NOT include badges, emojis, or marketing language. Keep the tone direct and technical. Target audience: security researchers and platform defenders who understand prompt injection.

Do NOT include a Contributing section or Code of Conduct — this is early-stage.
  </action>
  <verify>
    <automated>test -s README.md && wc -l README.md | awk '{if ($1 >= 60) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>README.md exists with project description, installation, usage for init+generate, proof levels, current status showing Phase 1 complete, safety model, and no placeholder/stub content remaining</done>
</task>

</tasks>

<verification>
- README.md is more than 60 lines
- Contains sections: installation, usage, status
- Mentions `honeyprompt init` and `honeyprompt generate`
- Shows Phase 1 as complete
- No emojis, no badges, no marketing fluff
</verification>

<success_criteria>
A GitHub visitor landing on the repo understands what HoneyPrompt does, how to build and run it (init + generate), what proof levels it supports, and that Phases 2-4 are planned.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-vo4-update-readme-md-as-needed-and-keep-it-u/260328-vo4-SUMMARY.md`
</output>
