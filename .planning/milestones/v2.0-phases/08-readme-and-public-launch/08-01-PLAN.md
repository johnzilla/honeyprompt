---
phase: 08-readme-and-public-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-03
must_haves:
  truths:
    - "A security researcher landing cold can understand the value prop in the first 10 lines"
    - "README links to live demo at honeyprompt.sh near the top"
    - "Quick-start for test-agent is under 5 commands"
    - "Ethics & Safety section explains what HoneyPrompt does NOT do"
    - "All GitHub URLs point to johnzilla/honeyprompt"
    - "Project Status table shows phases 1-7 complete"
  artifacts:
    - path: "README.md"
      provides: "Polished README with value prop, demo link, ethics section, correct URLs"
      contains: "honeyprompt.sh"
  key_links:
    - from: "README.md"
      to: "https://honeyprompt.sh"
      via: "live demo link near top"
      pattern: "honeyprompt\\.sh"
    - from: "README.md"
      to: "https://github.com/johnzilla/honeyprompt"
      via: "all GitHub URLs"
      pattern: "github\\.com/johnzilla/honeyprompt"
---

<objective>
Polish the existing README.md with live demo link, updated project status, expanded ethics section, and corrected GitHub URLs.

Purpose: Make the README compelling for a security researcher arriving cold — they should understand the value, see the live demo, and know how to install in under 30 seconds of reading.
Output: Updated README.md ready for public launch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-readme-and-public-launch/08-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live demo link and fix all GitHub URLs</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
Make these targeted edits to README.md (do NOT rewrite from scratch — polish what exists):

1. **Add live demo link near the top** (per D-03): After the one-line description ("Terminal-first security tool..."), add a line:

   **Live Demo:** [honeyprompt.sh](https://honeyprompt.sh) — a live honeypot serving canary payloads to AI agents right now.

   Use discretion on whether to also add a badge/shield at the very top (e.g., a "Live Demo" badge linking to honeyprompt.sh). A simple link is fine if a badge feels noisy.

2. **Fix all GitHub URLs** (per D-04): Replace every occurrence of `github.com/honeyprompt/honeyprompt` with `github.com/johnzilla/honeyprompt`. This affects:
   - Installation section (prebuilt binary download URLs, cargo install URL, git clone URL)
   - Any other references

Search the entire file for `honeyprompt/honeyprompt` to catch all instances.
  </action>
  <verify>
    <automated>grep -c "honeyprompt/honeyprompt" README.md | grep -q "^0$" && grep -q "honeyprompt.sh" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- Zero occurrences of `honeyprompt/honeyprompt` in README.md
- All GitHub URLs use `johnzilla/honeyprompt`
- Live demo link to honeyprompt.sh appears in the first 10 lines
  </acceptance_criteria>
  <done>README has live demo link near top and all URLs point to johnzilla/honeyprompt</done>
</task>

<task type="auto">
  <name>Task 2: Update project status and expand ethics section</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
Continue editing README.md with these changes:

1. **Update Project Status table** (per D-02): Update the table so:
   - Phase 6 (Release Infrastructure) status changes from "In Progress" to "Complete"
   - Add Phase 7 row: "Live Demo Deployment — `honeyprompt serve` running at honeyprompt.sh with HTTPS and auto-restart" with status "Complete"
   - Add Phase 8 row: "Public Launch — README polish, ethics section, discoverability" with status "Complete"

2. **Expand Safety Model to Ethics and Safety** (per D-01): Rename the "## Safety Model" heading to "## Ethics and Safety". Add an introductory paragraph BEFORE the existing bullet points. The paragraph should clearly state what HoneyPrompt is NOT:
   - Not an exploit tool or vulnerability scanner
   - Not a surveillance instrument
   - Not designed to collect secrets, credentials, or private data
   - Its purpose is to produce verifiable evidence of AI agent compliance behavior

   Keep all existing bullet points unchanged after this paragraph.

3. **Verify quick-start clarity** (per LAUNCH-01): The existing Usage section already covers test-agent in under 5 commands. Confirm this is the case — if the test-agent section needs a simpler "Quick Start" callout at the top of Usage, add one. Otherwise leave Usage as-is.
  </action>
  <verify>
    <automated>grep -q "Ethics" README.md && grep -q "Phase 7" README.md && grep -q "not an exploit" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- Section heading reads "Ethics and Safety" (not "Safety Model")
- Ethics intro paragraph contains "not an exploit" or equivalent negative framing
- Project Status table has rows for phases 1 through 8
- Phase 6 and 7 show "Complete" status
  </acceptance_criteria>
  <done>Project status reflects all phases, ethics section expanded with what-HoneyPrompt-is-NOT framing</done>
</task>

</tasks>

<verification>
Full README verification:
1. `grep -c "honeyprompt/honeyprompt" README.md` returns 0
2. `grep "honeyprompt.sh" README.md` shows live demo link
3. `grep "Ethics" README.md` shows renamed section
4. `grep "Phase 7" README.md` shows deployment row
5. `grep "johnzilla" README.md` shows corrected URLs
</verification>

<success_criteria>
- README.md passes all five grep checks above
- A cold reader sees: value prop, live demo link, install options, ethics framing — all within the first scroll
- No new files created — this is a polish pass on an existing README
</success_criteria>

<output>
After completion, create `.planning/phases/08-readme-and-public-launch/08-01-SUMMARY.md`
</output>
