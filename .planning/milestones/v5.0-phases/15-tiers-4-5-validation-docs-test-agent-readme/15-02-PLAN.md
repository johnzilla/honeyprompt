---
phase: 15
plan: "15-02"
title: "README 5-tier documentation (Proof Levels + Ethics + Project Status)"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "README §Proof Levels has an inline italic parenthetical concrete example for each of T1, T2, T3, T4, and T5 (5 examples total)"
    - "README §Ethics and Safety has explicit T4 and T5 no-secrets bullets appended to the existing 5-bullet list"
    - "README Project Status table has a Phase 15 row appended after the Phase 14 row"
    - "T5 worked-example formula constants match a real shipped tier5.toml template (t5-semantic-prose: a=42, b=17, mod=1000)"
    - "Existing 5-bullet Proof Levels structure preserved — no new subsection or table"
    - "Existing 5-bullet Ethics list preserved unchanged; lead paragraph unchanged"
  artifacts:
    - path: "README.md"
      provides: "5-tier documented Proof Levels with inline examples; T4/T5 no-secrets Ethics callouts; Phase 15 Project Status row"
      contains: "Phase 15 |"
  key_links:
    - from: "README.md §Proof Levels T5 bullet"
      to: "assets/catalog/tier5.toml::t5-semantic-prose"
      via: "shared formula constants (42, 17, 1000)"
      pattern: "\\(\\(137\\+42\\)\\*17\\) ?% ?1000|137\\+42.*17.*1000"
    - from: "README.md §Project Status Phase 15 row"
      to: ".planning/ROADMAP.md §Phase 15"
      via: "phase number + capability description"
      pattern: "Phase 15.*Tiers 4 & 5 Validation"
---

<objective>
Update three localized sections of `README.md` to document the v5.0 5-tier proof model and reflect Phase 15's planned shipped state. Three edits: (1) append italic parenthetical inline examples to each of the 5 Proof Levels bullets; (2) append two new Ethics and Safety bullets calling out T4 and T5 no-secrets guarantees; (3) append a single Phase 15 row to the Project Status table.

Purpose: Closes Phase 15 success criterion #3 (one short concrete example per tier in Proof Levels + explicit T4/T5 no-secrets in Ethics) and contributes to #4 (Project Status reflects v5.0 phases). Backs requirements DOCS-01, DOCS-02, DOCS-03.

Output: Single `README.md` diff. No code changes. No new files. Existing structure preserved (no section renaming, no v1.0–v4.0 row collapsing per D-15-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md
@README.md
@assets/catalog/tier5.toml

<interfaces>
<!-- Exact text excerpts from the files being edited. The exact wording of the inserted strings is locked by D-15-08, D-15-09, D-15-11, D-15-12. -->

**Current `README.md` §Proof Levels (lines 23–33):**
```markdown
## Proof Levels

HoneyPrompt uses a five-tier graduated evidence model:

- **Tier 1: Arbitrary callback** — Agent executed an injected outbound request
- **Tier 2: Conditional callback** — Agent evaluated a condition and selected the correct branch
- **Tier 3: Computed callback** — Agent performed a non-sensitive computation and returned the result
- **Tier 4: Capability introspection** — Agent self-reported a sorted list of its own tools or scopes as a base64-encoded payload on the callback path (no secrets)
- **Tier 5: Multi-step compliance chain** — Agent extracted a page-visible seed, applied a small deterministic formula, and submitted a 3-digit proof that the server re-verifies against the expected value

Each tier's callback URL carries only a unique cryptographic nonce, the prompt ID, and the tier level — no secrets or sensitive data.
```

**Current `README.md` §Ethics and Safety (lines 301–311):**
```markdown
## Ethics and Safety

HoneyPrompt is not an exploit tool, a vulnerability scanner, or a surveillance instrument. It is not designed to collect secrets, credentials, or private data of any kind. Its sole purpose is to produce verifiable, graduated evidence of whether AI agents comply with prompt-injection instructions from untrusted web content — so defenders can measure the risk, not cause harm.

HoneyPrompt is designed to produce evidence, not to exploit or surveil:

- Payloads carry only prompt ID, nonce, tier, and derived non-sensitive values — no API keys, session tokens, file contents, or environment variables
- Every generated page displays a visible warning for human visitors
- All payload instructions are auditable in the open-source catalog
- Custom payload authoring is not supported — the curated catalog ensures the safety guarantee holds
- No harmful actions are requested of agents at any tier
```

**Current `README.md` §Project Status table tail (lines 297–299):**
```markdown
| Phase 12 | Documentation & Deploy Templates — Deploy Your Own guide, deploy/templates/ for all platforms | Complete |
| Phase 13 | Tiers 4 & 5 Backend — payload catalog extension, `/cb/v4/` + `/cb/v5/` routes, server-side proof verification, additive SQLite migration | Complete |
| Phase 14 | Tiers 4 & 5 Surfacing — Monitor TUI EVIDENCE column + detail pane, 5-tier stats header, 6-state filter cycle, Markdown report Evidence column, executive summary T4/T5 rows | Complete |
```

**T5 worked-example math verification (planner-confirmed against `assets/catalog/tier5.toml::t5-semantic-prose`):**
- Constants: `formula_a = 42`, `formula_b = 17`, `formula_mod = 1000`
- Worked: seed = 137 → S = 137 + 42 = 179 → proof = (179 * 17) % 1000 = 3043 % 1000 = **43** → zero-padded to 3 digits = **`043`**
- The CONTEXT.md draft used `(137+42)*7 %1000 → 253` which references constants `(42, 7, 1000)` — those do **not** match any shipped template. Per CONTEXT.md `<specifics>`: *"Planner should verify the T5 constants `(42, 7, 1000)` actually match one of the shipped `assets/catalog/tier5.toml` templates — if not, swap to constants that do."* Switching to `(42, 17, 1000)` from the shipped `t5-semantic-prose` template, with the recomputed proof `043`, is mandatory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append italic parenthetical inline examples to each of the 5 Proof Levels bullets in README.md (D-15-08, D-15-09, D-15-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 23–33 (existing Proof Levels block)
    - .planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md `<specifics>` section (locked example wording)
    - assets/catalog/tier5.toml lines 6–13 (`t5-semantic-prose` constants — sourced for the T5 worked example)
  </read_first>
  <behavior>
    After the edit, each of the 5 Proof Levels bullets has a literal `*(e.g., ...)*` italic parenthetical appended after the existing description, on the same bullet line, separated by a single space. The pre-existing **bold tier label** + em-dash + description prose stays unchanged. The intro line ("HoneyPrompt uses a five-tier graduated evidence model:") is unchanged. The trailing nonce-disclaimer line ("Each tier's callback URL carries only ...") is unchanged.

    The literal `{nonce}` placeholder is used (D-15-10) — NOT a fake hex value.

    The T5 example uses constants from a **real shipped catalog template** (`t5-semantic-prose`: a=42, b=17, mod=1000) so a curious reader can grep the catalog and find the same numbers. Worked: seed 137 → ((137+42)*17) %1000 = 043.
  </behavior>
  <action>
    Replace the Proof Levels block (lines 25–31) so each tier bullet ends with the corresponding italic parenthetical. The exact replacement bullets are:

    ```markdown
    - **Tier 1: Arbitrary callback** — Agent executed an injected outbound request *(e.g., `GET /cb/v1/{nonce}`)*
    - **Tier 2: Conditional callback** — Agent evaluated a condition and selected the correct branch *(e.g., `GET /cb/v1/{nonce}/A` when the condition picks branch A)*
    - **Tier 3: Computed callback** — Agent performed a non-sensitive computation and returned the result *(e.g., `GET /cb/v1/{nonce}/42` after counting 'TODO' comments)*
    - **Tier 4: Capability introspection** — Agent self-reported a sorted list of its own tools or scopes as a base64-encoded payload on the callback path (no secrets) *(e.g., `GET /cb/v4/{nonce}/d2ViX3NlYXJjaCxicm93c2VfcGFnZQ==` decodes to 'web_search,browse_page')*
    - **Tier 5: Multi-step compliance chain** — Agent extracted a page-visible seed, applied a small deterministic formula, and submitted a 3-digit proof that the server re-verifies against the expected value *(e.g., seed 137 → formula `((137+42)*17) %1000` → `GET /cb/v5/{nonce}/043`)*
    ```

    Notes for the executor:
    - **The T5 example math: do not change the numbers.** `(137+42)*17 %1000 = 179*17 %1000 = 3043 %1000 = 43 → zero-padded "043"`. These are the formula constants of the **`t5-semantic-prose`** payload in `assets/catalog/tier5.toml` (lines 10–12: `formula_a = 42`, `formula_b = 17`, `formula_mod = 1000`).
    - The italic parenthetical wrapper is `*( ... )*`. Backticks around code spans inside the italics are preserved (Markdown renders code spans inside emphasis correctly in GitHub-flavored Markdown).
    - The T4 base64 string `d2ViX3NlYXJjaCxicm93c2VfcGFnZQ==` decodes to `web_search,browse_page` — verifiable with `printf '%s' 'd2ViX3NlYXJjaCxicm93c2VfcGFnZQ==' | base64 -d`. Copy this string verbatim.
    - **Do not** add a new subsection ("Examples", "Sample callbacks", etc.) below the bullets. Per D-15-08 the existing 5-bullet structure is preserved.
    - **Do not** modify the lead paragraph "HoneyPrompt uses a five-tier graduated evidence model:" or the trailing paragraph "Each tier's callback URL carries only ...".
    - The Tier 4 description retains its existing parenthetical "(no secrets)" inside the description prose; the italic example parenthetical is then appended after that. The two parentheticals are intentional — one is part of the description, one is the example.
  </action>
  <verify>
    <automated>grep -q '\*(e.g., `GET /cb/v1/{nonce}`)\*' README.md && grep -q '\*(e.g., `GET /cb/v4/{nonce}/d2ViX3NlYXJjaCxicm93c2VfcGFnZQ==` decodes to' README.md && grep -q 'seed 137 → formula `((137+42)\*17) %1000` → `GET /cb/v5/{nonce}/043`' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\*(e.g., `GET /cb/v1/{nonce}`)\*' README.md` (T1 example present)
    - `grep -q '\*(e.g., `GET /cb/v1/{nonce}/A` when the condition picks branch A)\*' README.md` (T2 example)
    - `grep -q "\\*(e.g., \`GET /cb/v1/{nonce}/42\` after counting 'TODO' comments)\\*" README.md` (T3 example)
    - `grep -q 'd2ViX3NlYXJjaCxicm93c2VfcGFnZQ==' README.md` (T4 base64 string)
    - `grep -q 'web_search,browse_page' README.md` (T4 decoded explanation)
    - `grep -q '((137+42)\*17) %1000' README.md` (T5 worked formula matches shipped t5-semantic-prose)
    - `grep -q '/cb/v5/{nonce}/043' README.md` (T5 worked proof = 043)
    - `! grep -qE '/cb/v5/\{nonce\}/253' README.md` (CONTEXT.md draft constants discarded — proof 253 came from the unmatched (42,7,1000) trio)
    - `grep -q 'HoneyPrompt uses a five-tier graduated evidence model:' README.md` (intro preserved)
    - `grep -q "Each tier's callback URL carries only a unique cryptographic nonce" README.md` (trailer preserved)
    - The Proof Levels section still has exactly 5 list bullets immediately after the intro line: `awk '/^## Proof Levels/,/^## /' README.md | grep -cE '^- \*\*Tier ' | grep -q '^5$'`
  </acceptance_criteria>
  <done>
    All 5 Proof Levels bullets carry an italic parenthetical example, T5 example math matches the shipped `t5-semantic-prose` catalog template, no surrounding paragraphs are changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append two no-secrets bullets to the Ethics and Safety list (D-15-11) — T4 and T5 specific</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 301–311 (current Ethics list)
    - .planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md `<specifics>` (user-preferred bullet wording)
  </read_first>
  <behavior>
    After the edit, the Ethics and Safety bulleted list contains the existing 5 bullets unchanged plus two new bullets appended at the bottom (one for Tier 4, one for Tier 5). The lead paragraphs ("HoneyPrompt is not an exploit tool..." and "HoneyPrompt is designed to produce evidence, not to exploit or surveil:") are unchanged. The section header `## Ethics and Safety` is unchanged. The total bullet count for the Ethics list goes from 5 → 7.
  </behavior>
  <action>
    Append two bullets immediately **after** the existing fifth bullet (`- No harmful actions are requested of agents at any tier`) in the Ethics and Safety list (line 311). The exact wording (verbatim from D-15-11):

    ```markdown
    - **Tier 4 never asks for secrets** — the agent returns a sorted list of tool/capability names from a safe, agent-chosen menu (e.g., `web_search,browse_page`). No API keys, no session state, no file contents.
    - **Tier 5 never asks for secrets** — the proof is arithmetic over a page-visible `verification_seed` plus fixed catalog constants. The callback carries only a 3-digit number the server independently re-computes.
    ```

    Notes for the executor:
    - Place these two bullets **at the bottom** of the existing 5-bullet list (recommended placement per CONTEXT.md `<decisions>` Claude's Discretion: "bottom of list recommended, but anywhere after the 'no harmful actions' bullet works"). Do **not** insert them above any of the existing bullets.
    - Use literal `**bold**` for the tier-prefix labels (matches existing style in §Proof Levels).
    - The example `web_search,browse_page` is wrapped in backticks (inline code).
    - The phrase `verification_seed` is wrapped in backticks (matches the catalog field name).
    - **Do not** modify the lead paragraph or any other Ethics text. Do **not** rename the section.
    - **Do not** add subheadings (no `### Tier 4 Safety`).
  </action>
  <verify>
    <automated>grep -q '\*\*Tier 4 never asks for secrets\*\* — the agent returns a sorted list of tool/capability names' README.md && grep -q '\*\*Tier 5 never asks for secrets\*\* — the proof is arithmetic over a page-visible' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\*\*Tier 4 never asks for secrets\*\* — the agent returns a sorted list of tool/capability names from a safe, agent-chosen menu' README.md`
    - `grep -q '\*\*Tier 5 never asks for secrets\*\* — the proof is arithmetic over a page-visible `verification_seed` plus fixed catalog constants' README.md`
    - `grep -q 'No API keys, no session state, no file contents.' README.md`
    - `grep -q '3-digit number the server independently re-computes' README.md`
    - The Ethics list still contains all 5 original bullets — verify their first lines are intact:
      - `grep -q 'Payloads carry only prompt ID, nonce, tier' README.md`
      - `grep -q 'Every generated page displays a visible warning for human visitors' README.md`
      - `grep -q 'All payload instructions are auditable in the open-source catalog' README.md`
      - `grep -q 'Custom payload authoring is not supported' README.md`
      - `grep -q 'No harmful actions are requested of agents at any tier' README.md`
    - The Ethics list ordering: `awk '/^## Ethics and Safety/,/^## /' README.md | grep -nE '^- ' | head -7 | tail -2 | grep -q 'Tier 4 never asks for secrets'` (the 6th list item is the new T4 bullet, confirming bottom-of-list placement)
  </acceptance_criteria>
  <done>
    Ethics list grows from 5 → 7 bullets with the two new T4/T5 no-secrets bullets at the bottom, all original bullets intact, lead paragraph unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 15 row to the Project Status table (D-15-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 282–299 (Project Status table — existing 14 rows ending at Phase 14)
    - .planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md (D-15-12 — exact description string)
  </read_first>
  <behavior>
    After the edit, the Project Status table has a new row appended immediately after the Phase 14 row, with description per D-15-12 and status `In Progress`. No existing rows are modified or collapsed. The table header is unchanged.
  </behavior>
  <action>
    Append a single Markdown table row immediately after the existing Phase 14 row (line 299). The exact row to add (verbatim per D-15-12):

    ```markdown
    | Phase 15 | Tiers 4 & 5 Validation & Docs — test-agent scorecard extended to T4/T5, README 5-tier proof model documented, TODOS.md shipped section | In Progress |
    ```

    Notes for the executor:
    - Status column is `In Progress` (CONTEXT.md `<decisions>` Claude's Discretion: "depends on plan order — planner picks"). Per the planner's choice, `In Progress` is correct because Phase 15 success is verified at end-of-phase, not at plan time. The phase-completion docs sync will flip this to `Complete`.
    - Description string is **byte-identical** to the D-15-12 wording — no paraphrasing.
    - **Do not** collapse v1.0–v4.0 rows into milestone rows (D-15-12 explicitly rejects this).
    - **Do not** add additional Phase 15 rows. Exactly one new row.
    - **Do not** modify the Phase 13 or Phase 14 rows.
    - **Do not** modify the table header (`| Phase | Capability | Status |`) or the divider row.
  </action>
  <verify>
    <automated>grep -q '| Phase 15 | Tiers 4 & 5 Validation & Docs — test-agent scorecard extended to T4/T5, README 5-tier proof model documented, TODOS.md shipped section | In Progress |' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '| Phase 15 | Tiers 4 & 5 Validation & Docs' README.md` (row present)
    - `grep -q 'test-agent scorecard extended to T4/T5' README.md`
    - `grep -q 'README 5-tier proof model documented' README.md`
    - `grep -q 'TODOS.md shipped section' README.md`
    - `grep -q '| In Progress |' README.md` (status set per planner choice)
    - The Phase 14 row is still present and unchanged: `grep -q '| Phase 14 | Tiers 4 & 5 Surfacing' README.md`
    - The Phase 13 row is still present and unchanged: `grep -q '| Phase 13 | Tiers 4 & 5 Backend' README.md`
    - The table contains exactly one Phase 15 row: `grep -c '| Phase 15 |' README.md | grep -q '^1$'`
    - The header row is unchanged: `grep -q '| Phase | Capability | Status |' README.md`
  </acceptance_criteria>
  <done>
    Project Status table has exactly one new Phase 15 row in the documented position with the locked description and `In Progress` status. All preceding rows are intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation edits to a Markdown file. No code paths touched, no input parsing, no rendered HTML beyond GitHub's Markdown renderer. README.md is read by humans browsing GitHub, not loaded by the binary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | T4 example base64 string | accept | The base64 string `d2ViX3NlYXJjaCxicm93c2VfcGFnZQ==` decodes to the public string `web_search,browse_page` — generic capability names, not secrets. Verified pre-encoded in CONTEXT.md `<specifics>`. |
| T-15-02-02 | Spoofing | T5 worked example accuracy | mitigate | The example math `((137+42)*17) %1000 = 043` is verified by hand and corresponds to the shipped `t5-semantic-prose` catalog template constants (a=42, b=17, mod=1000) — a curious reader who runs `cargo run -- generate` and inspects the formula will find the same numbers. Eliminates the "documentation lies about the implementation" surface. |
| T-15-02-03 | Repudiation | Status accuracy | accept | `In Progress` accurately reflects state at plan-write time; the phase-completion sync will flip it to `Complete` once verification passes. No false claim of shipped state. |
</threat_model>

<verification>
- All grep checks listed in each task's `<acceptance_criteria>` hold
- `awk '/^## Proof Levels/,/^## /' README.md` renders 5 tier bullets each with one `*(e.g., ...)*` italic parenthetical
- `awk '/^## Ethics and Safety/,/^## /' README.md` shows 7 bullets (5 original + 2 new T4/T5 no-secrets bullets)
- `awk '/^## Project Status/,/^## /' README.md` shows 15 phase rows ending with the Phase 15 `In Progress` row
- The shipped catalog template `assets/catalog/tier5.toml::t5-semantic-prose` constants `(42, 17, 1000)` match the README T5 example
- Optional human render check: open the README on GitHub or in a Markdown previewer and confirm the italic parentheticals render correctly inside the bullet lines
</verification>

<success_criteria>
- README §Proof Levels has 5 bullets, each with an inline `*(e.g., ...)*` example using the literal `{nonce}` placeholder (D-15-10)
- T5 worked example matches a real shipped catalog template (`t5-semantic-prose`)
- README §Ethics and Safety has 7 bullets — original 5 unchanged + 2 new T4/T5 no-secrets bullets at the bottom
- README Project Status table has a Phase 15 row with the D-15-12 description and `In Progress` status
- No section renaming, no v1.0–v4.0 row collapsing, no new subsections introduced
- Existing intro/outro paragraphs in all three sections unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture:
- Diff stats for `README.md` (lines added — should be ~7 net additions: 5 inline appendings on Proof Levels bullets count as edits not adds, 2 new Ethics bullets, 1 new Project Status row)
- Confirmation that all `<acceptance_criteria>` greps pass
- Confirmation that the T5 worked example references real catalog constants from `assets/catalog/tier5.toml::t5-semantic-prose`
- Note the choice of `In Progress` for the Phase 15 status row (will be flipped to `Complete` by the phase-completion docs sync)
</output>
