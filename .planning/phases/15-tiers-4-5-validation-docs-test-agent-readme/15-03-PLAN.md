---
phase: 15
plan: "15-03"
title: "TODOS.md ## Shipped section"
type: execute
wave: 1
depends_on: []
files_modified:
  - TODOS.md
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "TODOS.md has a new ## Shipped section above the existing security-email TODO"
    - "The Shipped section contains exactly two entries — one for Tier 4, one for Tier 5 — both referencing v5.0 (Phases 13–15)"
    - "The pre-existing ## Set up project-specific disclosure email entry is unchanged"
    - "Each Shipped entry references the v5.0 phase numbers (13–15) and the relevant callback route (/cb/v4/ or /cb/v5/)"
  artifacts:
    - path: "TODOS.md"
      provides: "## Shipped section with T4 + T5 entries; existing security-email TODO preserved untouched"
      contains: "## Shipped"
  key_links:
    - from: "TODOS.md ## Shipped Tier 4 entry"
      to: ".planning/REQUIREMENTS.md DOCS-04"
      via: "explicit shipped-state declaration with phase reference"
      pattern: "shipped in v5\\.0 \\(Phases 13[–-]15\\)"
    - from: "TODOS.md ## Shipped Tier 5 entry"
      to: "src/server/mod.rs::/cb/v5/{nonce}/{proof}"
      via: "callback route mention in the shipped entry"
      pattern: "/cb/v5/\\{nonce\\}/\\{proof\\}"
---

<objective>
Add a `## Shipped` section to `TODOS.md` containing two entries — one for Tier 4 (Capability Introspection) and one for Tier 5 (Multi-step Compliance Chain) — both shipped in v5.0 (Phases 13–15). Place the new section ABOVE the existing `## Set up project-specific disclosure email` TODO, preserving that entry intact.

Purpose: Closes Phase 15 success criterion #4 (TODOS.md updated so T4/T5 no longer appear under "future" — they appear under "shipped" with v5.0 phase references). Backs requirement DOCS-04.

Output: Single `TODOS.md` diff. No code changes. The file structure remains a flat sequence of `##` H2 sections with the Shipped section first, then the security-email TODO unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md
@TODOS.md

<interfaces>
<!-- Current full content of TODOS.md (10 lines, 1 entry). The Shipped section will be inserted above the existing H2. -->

Current `TODOS.md`:
```markdown
# TODOS

## Set up project-specific disclosure email
- **What:** Configure security@honeyprompt.dev (or similar) and update security.txt Contact field
- **Why:** RFC 9116 security.txt currently uses GitHub Security Advisories URL as contact. A dedicated email is more professional and discoverable.
- **Pros:** Direct communication channel for disclosure. More trusted by security researchers.
- **Cons:** Requires DNS MX records or email forwarding service. Ongoing maintenance.
- **Context:** security.txt ships with GitHub Security Advisories URL as v1 contact. This TODO captures the upgrade to a real email once honeyprompt.dev DNS is configured. MX records or a forwarding service (e.g., ImprovMX, Cloudflare Email Routing) needed.
- **Depends on:** honeyprompt.dev DNS setup (this plan)
- **Added:** 2026-03-31 via /plan-eng-review
```

**Format choice (Claude's Discretion per CONTEXT.md):** inline `**bold**` prefixes for the two entries — chosen for compactness and consistency with the single-entry, list-bullet pattern of the existing security-email TODO. Sub-headers (`### Tier 4` / `### Tier 5`) are an alternative but produce more visual weight than this short two-entry list warrants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ## Shipped section above the existing TODO entry, with Tier 4 and Tier 5 bullets per D-15-13</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md (full 10-line file)
    - .planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-CONTEXT.md (D-15-13 — exact bullet wording for both T4 and T5; CONTEXT.md Claude's Discretion section permits inline `**bold**` over `### Tier 4` sub-headers)
  </read_first>
  <behavior>
    After the edit, the file structure is:

    ```
    # TODOS

    ## Shipped

    - **Tier 4: Capability Introspection** — shipped in v5.0 (Phases 13–15). ...
    - **Tier 5: Multi-step Compliance Chain** — shipped in v5.0 (Phases 13–15). ...

    ## Set up project-specific disclosure email
    - **What:** ... (unchanged)
    ...
    - **Added:** 2026-03-31 via /plan-eng-review
    ```

    The existing `## Set up project-specific disclosure email` block (8 list-bullet lines from the original `## Set up...` header through the `**Added:**` line) is byte-identical to the pre-edit form.
  </behavior>
  <action>
    Edit `TODOS.md` using the Edit tool to insert a new `## Shipped` section between the H1 title (`# TODOS`) and the existing `## Set up project-specific disclosure email` section.

    Replace this exact pre-edit block:

    ```
    # TODOS

    ## Set up project-specific disclosure email
    ```

    with this exact post-edit block:

    ```
    # TODOS

    ## Shipped

    - **Tier 4: Capability Introspection** — shipped in v5.0 (Phases 13–15). Agents self-report sorted, base64-encoded tool lists via `/cb/v4/{nonce}/{b64_list}`.
    - **Tier 5: Multi-step Compliance Chain** — shipped in v5.0 (Phases 13–15). Agents extract a page-visible seed, apply a deterministic formula, and submit a 3-digit proof the server re-verifies via `/cb/v5/{nonce}/{proof}`.

    ## Set up project-specific disclosure email
    ```

    Notes for the executor:
    - Use the literal em-dash `—` (U+2014) as the separator between the bold prefix and the description text. Matches the existing README and CONTEXT.md style. Do NOT substitute an ASCII hyphen.
    - Use the literal en-dash `–` (U+2013) inside `Phases 13–15` to match planning artifacts (CONTEXT.md and ROADMAP.md both use the en-dash inside phase ranges). If your editor inserts an ASCII hyphen `13-15` instead, that is acceptable — note the substitution in the SUMMARY. Both forms satisfy the acceptance criterion below (which accepts either Unicode dash via character class).
    - Backticks around `/cb/v4/{nonce}/{b64_list}` and `/cb/v5/{nonce}/{proof}` mark them as inline code spans.
    - The H2 header is exactly `## Shipped` — no trailing colon, no decorations.
    - Do NOT add a third bullet, a date stamp, or a "Removed from future" note. Exactly two entries per D-15-13.
    - Do NOT modify the H1 `# TODOS` title.
    - Do NOT modify any line of the existing `## Set up project-specific disclosure email` block (its 8 lines, including the H2 header line, must be unchanged).
    - Confirm post-edit by running `git diff TODOS.md` — the diff should show only insertions in a single hunk above the existing H2; there should be zero deletions and zero modifications to pre-existing lines.
  </action>
  <verify>
    <automated>grep -q '^## Shipped$' TODOS.md && grep -q 'Tier 4: Capability Introspection' TODOS.md && grep -q 'Tier 5: Multi-step Compliance Chain' TODOS.md && grep -q 'Set up project-specific disclosure email' TODOS.md && grep -qE 'shipped in v5\.0 \(Phases 13[–-]15\)' TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Shipped$' TODOS.md` (new section header present)
    - `grep -q '\*\*Tier 4: Capability Introspection\*\*' TODOS.md` (T4 entry bolded prefix)
    - `grep -q '\*\*Tier 5: Multi-step Compliance Chain\*\*' TODOS.md` (T5 entry bolded prefix)
    - `grep -qE 'shipped in v5\.0 \(Phases 13[–-]15\)' TODOS.md` (v5.0 phase reference present in at least one entry; `[–-]` accepts either en-dash or ASCII hyphen)
    - `grep -q '/cb/v4/{nonce}/{b64_list}' TODOS.md` (T4 entry references the actual callback route)
    - `grep -q '/cb/v5/{nonce}/{proof}' TODOS.md` (T5 entry references the actual callback route)
    - `grep -q '## Set up project-specific disclosure email' TODOS.md` (existing TODO H2 preserved)
    - `grep -q 'security@honeyprompt.dev' TODOS.md` (existing TODO body line 4 preserved)
    - `grep -q 'Added:\*\* 2026-03-31 via /plan-eng-review' TODOS.md` (existing TODO last line preserved)
    - `grep -c '^## Shipped$' TODOS.md` returns `1` (exactly one Shipped section)
    - `grep -cE '^- \*\*Tier [45]:' TODOS.md` returns `2` (exactly two tier bullets — no extras)
    - `git diff --stat TODOS.md` shows only added lines (no deletions): `git diff TODOS.md | grep -E '^-[^-]' | wc -l` returns `0`
  </acceptance_criteria>
  <done>
    `TODOS.md` has a new `## Shipped` H2 above the existing TODO with exactly two entries (Tier 4 and Tier 5) referencing v5.0 (Phases 13–15) and the relevant callback routes. The existing `## Set up project-specific disclosure email` block is byte-identical to the pre-edit form. `git diff TODOS.md` shows only additions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation edit to a Markdown file. No code paths, no input parsing, no rendered output beyond GitHub's Markdown view. TODOS.md is read by humans browsing the repo, not loaded by the binary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Repudiation | Shipped-state claim | accept | The "shipped in v5.0 (Phases 13–15)" claim is verifiable by readers via `.planning/ROADMAP.md` (Phase 13 and Phase 14 marked Complete; Phase 15 in progress). The claim is forward-looking for Phase 15's own scope but accurate for Phases 13–14 which have shipped. |
| T-15-03-02 | Tampering | Existing security-email TODO | mitigate | Plan acceptance criteria explicitly assert that all 8 lines of the existing TODO are preserved byte-identical (`git diff` shows zero deletions). The `Edit` tool's exact-match replacement contract enforces this at write time. |
</threat_model>

<verification>
- All grep checks listed in the task's `<acceptance_criteria>` hold
- `git diff TODOS.md` shows a single hunk of additions with zero deletions
- The file's H1 (`# TODOS`) is unchanged
- The pre-existing `## Set up project-specific disclosure email` block is byte-identical pre/post edit
- Total file line count grows by exactly 4 lines (1 H2 header + 1 blank + 2 bullet lines + 1 blank — net 4 new lines accounting for the blank between Shipped and the next H2)
</verification>

<success_criteria>
- TODOS.md has a `## Shipped` section above the existing security-email TODO
- The Shipped section contains exactly two entries — Tier 4 and Tier 5 — each referencing v5.0 (Phases 13–15) and the corresponding callback route (`/cb/v4/` or `/cb/v5/`)
- The existing security-email TODO is preserved unchanged
- `git diff TODOS.md` shows only additions, no deletions
</success_criteria>

<output>
After completion, create `.planning/phases/15-tiers-4-5-validation-docs-test-agent-readme/15-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture:
- Diff stats for `TODOS.md` (expected ~4 net additions, 0 deletions)
- Confirmation that all `<acceptance_criteria>` greps pass
- Confirmation of dash-character choice (em-dash + en-dash, or ASCII fallback) for the SUMMARY trail
- Note that DOCS-04 is fully closed by this single edit
</output>
