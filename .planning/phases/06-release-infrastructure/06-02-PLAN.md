---
phase: 06-release-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [REL-03]

must_haves:
  truths:
    - "README contains cargo install command for building from source"
    - "README contains prebuilt binary download instructions with curl example"
    - "README links to GitHub Releases page for binary downloads"
    - "Binary naming convention uses full Rust target triple"
  artifacts:
    - path: "README.md"
      provides: "Installation section with cargo install and binary download paths"
      contains: "cargo install"
  key_links:
    - from: "README.md"
      to: "GitHub Releases"
      via: "Download URL pattern"
      pattern: "github.com/honeyprompt/honeyprompt/releases"
---

<objective>
Add installation instructions to README.md covering both `cargo install` from source and prebuilt binary downloads from GitHub Releases.

Purpose: Security researchers need clear install paths without compiling from source (REL-03). The README is the first thing users see — install friction kills adoption.
Output: Updated `README.md` with a rewritten Installation section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-release-infrastructure/06-CONTEXT.md

<interfaces>
<!-- Binary naming convention from D-03 -->
Binary archives follow the pattern: honeyprompt-{target}.tar.gz
Targets: x86_64-unknown-linux-musl, aarch64-unknown-linux-musl, x86_64-apple-darwin, aarch64-apple-darwin

<!-- GitHub Releases URL pattern -->
https://github.com/honeyprompt/honeyprompt/releases/latest
Per-asset: https://github.com/honeyprompt/honeyprompt/releases/latest/download/honeyprompt-{target}.tar.gz
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README installation section</name>
  <read_first>README.md, .planning/phases/06-release-infrastructure/06-CONTEXT.md</read_first>
  <files>README.md</files>
  <action>
Replace the existing `## Installation` section in README.md with a comprehensive section covering two install paths. Keep all other sections of the README unchanged.

**New Installation section structure:**

```markdown
## Installation

### Prebuilt binaries

Download the latest release for your platform from [GitHub Releases](https://github.com/honeyprompt/honeyprompt/releases/latest).

| Platform | Binary |
|----------|--------|
| Linux x86_64 | `honeyprompt-x86_64-unknown-linux-musl.tar.gz` |
| Linux aarch64 | `honeyprompt-aarch64-unknown-linux-musl.tar.gz` |
| macOS x86_64 | `honeyprompt-x86_64-apple-darwin.tar.gz` |
| macOS Apple Silicon | `honeyprompt-aarch64-apple-darwin.tar.gz` |

Example (Linux x86_64):

\```sh
curl -LO https://github.com/honeyprompt/honeyprompt/releases/latest/download/honeyprompt-x86_64-unknown-linux-musl.tar.gz
tar xzf honeyprompt-x86_64-unknown-linux-musl.tar.gz
./honeyprompt --version
\```

### Build from source

Requires Rust toolchain (stable). Install from [rustup.rs](https://rustup.rs/) if not present.

\```sh
cargo install --git https://github.com/honeyprompt/honeyprompt
\```

Or clone and build:

\```sh
git clone https://github.com/honeyprompt/honeyprompt
cd honeyprompt
cargo build --release
\```
```

**Key decisions:**
- Per D-03, binary names use full Rust target triple (not shortened platform names)
- `cargo install --git` is the primary source install path (crates.io publish is deferred per Out of Scope)
- Linux binaries are musl-linked (static, portable) per D-05 — no glibc dependency notes needed
- Include a curl one-liner example for the most common platform (Linux x86_64)
- Do NOT include Windows (out of scope)
- Do NOT include Homebrew/shell installer (cargo-dist deferred)

**Also update the Project Status table** at the bottom of the README to reflect current state — Phases 1-4 complete, Phase 5 complete (test-agent), Phase 6 in progress. Add entries for `test-agent` subcommand in the Usage section or update the "Planned commands" list to reflect that `serve`, `monitor`, `report`, and `test-agent` are now implemented.
  </action>
  <verify>
    <automated>grep -q 'cargo install --git' README.md && echo "OK: cargo install present" && grep -q 'releases/latest/download' README.md && echo "OK: download URL present" && grep -q 'x86_64-unknown-linux-musl' README.md && echo "OK: target triple present" && grep -q 'aarch64-apple-darwin' README.md && echo "OK: macOS ARM present"</automated>
  </verify>
  <acceptance_criteria>
- README.md contains `cargo install --git https://github.com/honeyprompt/honeyprompt`
- README.md contains `https://github.com/honeyprompt/honeyprompt/releases/latest/download/honeyprompt-x86_64-unknown-linux-musl.tar.gz`
- README.md contains a platform table listing all four targets
- README.md contains a curl one-liner download example
- README.md contains `tar xzf` extraction step
- README.md does NOT mention Windows, Homebrew, or crates.io
- All other README sections (What This Is, Proof Levels, Usage, Safety Model, License) are preserved
- Project Status table updated to reflect completed phases
  </acceptance_criteria>
  <done>README contains both cargo install from git and prebuilt binary download instructions with platform table and curl example.</done>
</task>

</tasks>

<verification>
- README.md has two clear install paths: prebuilt binaries and build-from-source
- Platform table lists all four targets with correct archive names
- Download URLs point to GitHub Releases with correct asset naming convention
- No references to deferred items (crates.io, cargo-dist, Homebrew, Windows)
- Existing README content is preserved (not rewritten beyond installation section and status table)
</verification>

<success_criteria>
A security researcher reading the README can find and follow either install path: download a prebuilt binary for their platform, or build from source via cargo install --git. Both paths are complete with copy-pasteable commands.
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-infrastructure/06-02-SUMMARY.md`
</output>
