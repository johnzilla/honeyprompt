---
phase: 04-report-and-landing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/honeyprompt.toml
  - landing/output/.gitkeep
  - .gitignore
autonomous: true
requirements: [LAND-01]

must_haves:
  truths:
    - "A honeyprompt.toml config exists in landing/ directory configured for honeyprompt.sh"
    - "The landing page config includes all three payload tiers (1, 2, 3)"
    - "The landing page callback URL points to honeyprompt.sh"
    - "The .honeyprompt/ runtime directory is gitignored so events.db is never committed"
  artifacts:
    - path: "landing/honeyprompt.toml"
      provides: "Landing page honeypot config for honeyprompt.sh"
      contains: "callback_base_url"
    - path: ".gitignore"
      provides: "Git ignore rules for runtime state"
      contains: ".honeyprompt/"
  key_links:
    - from: "landing/honeyprompt.toml"
      to: "src/generator/mod.rs"
      via: "honeyprompt generate landing/ uses existing generator pipeline"
      pattern: "callback_base_url"
---

<objective>
Create the honeyprompt.sh landing page configuration and project-level .gitignore. The landing page is produced by running `honeyprompt generate landing/` using the existing generator pipeline (dogfooding per D-07, D-08).

Purpose: The honeyprompt.sh landing page IS a honeypot page instrumented with canaries, proving the tool works on itself.
Output: landing/honeyprompt.toml config file, .gitignore for runtime state, landing/output/ directory placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/config/mod.rs
@src/generator/mod.rs

<interfaces>
<!-- Config struct fields that landing/honeyprompt.toml must match -->

From src/config/mod.rs:
```rust
pub struct Config {
    pub callback_base_url: String,   // e.g. "https://honeyprompt.sh"
    pub bind_address: String,        // e.g. "0.0.0.0:8080"
    pub tiers: Vec<u8>,              // [1, 2, 3]
    pub page_title: String,          // custom title
    pub theme: String,               // "default"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page config + .gitignore</name>
  <files>landing/honeyprompt.toml, landing/output/.gitkeep, .gitignore</files>
  <read_first>
    - src/config/mod.rs
  </read_first>
  <action>
    1. Create directory `landing/output/`.

    2. Create `landing/honeyprompt.toml` with the following content (per D-07, D-08):

    ```toml
    callback_base_url = "https://honeyprompt.sh"
    bind_address = "0.0.0.0:8080"
    tiers = [1, 2, 3]
    page_title = "HoneyPrompt — AI Agent Canary Detection"
    theme = "default"
    ```

    The callback_base_url points to honeyprompt.sh so canaries embedded in the landing page call back to the live site. All three tiers are included to demonstrate the full proof-level spectrum (D-08). The page_title identifies this as the HoneyPrompt project page.

    3. Create `landing/output/.gitkeep` as an empty placeholder. The actual output files are generated by running `honeyprompt generate landing/` and can optionally be committed after generation.

    4. Create `.gitignore` at the project root with:

    ```
    # Build artifacts
    /target/

    # Runtime state — never commit event databases
    .honeyprompt/

    # OS files
    .DS_Store
    ```

    The `.honeyprompt/` rule ensures that `landing/.honeyprompt/events.db` (created when running `honeyprompt generate landing/` or `honeyprompt serve landing/`) is never committed. This is runtime state, not a source artifact (per Pitfall 4 from research).

    5. Verify the config is parseable by the existing Config struct. Run:
    `cargo run -- generate landing/`
    This will create the landing page output files in `landing/output/`. If the generate command succeeds, the config is valid and the landing page contains embedded canaries (same as any generated honeypot, per D-08).

    6. After generation, verify the output contains the human-visible warning banner (GEN-02 compliance) and embedded canary payloads by checking that `landing/output/index.html` contains both "SECURITY RESEARCH CANARY" (or the warning text) and callback URLs pointing to honeyprompt.sh.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/honeyprompt && cargo run -- generate landing/ 2>&1 && test -f landing/output/index.html && grep -q "honeyprompt.sh" landing/output/index.html && echo "PASS: landing page generated with honeyprompt.sh callbacks"</automated>
  </verify>
  <acceptance_criteria>
    - test -f landing/honeyprompt.toml
    - grep -q 'callback_base_url = "https://honeyprompt.sh"' landing/honeyprompt.toml
    - grep -q 'tiers = \[1, 2, 3\]' landing/honeyprompt.toml
    - test -f .gitignore
    - grep -q ".honeyprompt/" .gitignore
    - grep -q "/target/" .gitignore
    - test -f landing/output/index.html
    - grep -q "honeyprompt.sh" landing/output/index.html
  </acceptance_criteria>
  <done>
    - landing/honeyprompt.toml exists with honeyprompt.sh callback URL and all 3 tiers (LAND-01)
    - Running `honeyprompt generate landing/` produces a valid honeypot page with embedded canaries pointing to honeyprompt.sh (D-07, D-08)
    - .gitignore prevents .honeyprompt/ runtime state from being committed (Pitfall 4)
    - The generated landing page contains the same canaries as any honeypot, proving the tool works on itself
  </done>
</task>

</tasks>

<verification>
1. `landing/honeyprompt.toml` parses correctly with `cargo run -- generate landing/`
2. `landing/output/index.html` exists and contains honeyprompt.sh callback URLs
3. `landing/output/index.html` contains the human-visible security warning
4. `.gitignore` excludes `.honeyprompt/` and `/target/`
5. `git status` does NOT show `landing/.honeyprompt/` as untracked
</verification>

<success_criteria>
- Landing page config committed to repository (LAND-01)
- Landing page is a real honeypot generated by the tool itself (D-07 dogfooding)
- Landing page contains embedded canaries with honeyprompt.sh callbacks (D-08)
- Runtime state (.honeyprompt/events.db) is gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/04-report-and-landing/04-02-SUMMARY.md`
</output>
