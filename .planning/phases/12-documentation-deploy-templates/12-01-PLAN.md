---
phase: 12-documentation-deploy-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "README contains a 'Deploy Your Own' section with install, setup, deploy, and verify steps"
    - "README references 'honeyprompt setup' and 'honeyprompt serve --domain' commands"
    - "README clearly separates the live demo (honeyprompt.sh) from self-hosted deployment"
    - "A reader can follow the Deploy Your Own section end-to-end without referencing any other file"
  artifacts:
    - path: "README.md"
      provides: "Complete project README with Deploy Your Own guide"
      contains: "## Deploy Your Own"
  key_links:
    - from: "README.md Deploy Your Own section"
      to: "honeyprompt setup and honeyprompt serve --domain commands"
      via: "step-by-step instructions"
      pattern: "honeyprompt setup|honeyprompt serve --domain"
    - from: "README.md"
      to: "deploy/templates/"
      via: "reference to template files for advanced users"
      pattern: "deploy/templates"
---

<objective>
Rewrite README.md to add a "Deploy Your Own" self-hosting guide and clearly separate the live demo persona from the self-hosted persona.

Purpose: Users arriving at the repo need a single path from "I want my own honeypot" to a running instance, using the Phase 11 commands (setup wizard, zero-config serve, --domain flag). The current README documents individual subcommands but has no deployment walkthrough and no mention of the v4.0 self-hosted workflow.

Output: Updated README.md with Deploy Your Own section, updated Project Status table, persona separation throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with Deploy Your Own guide and persona separation</name>
  <files>README.md</files>
  <read_first>README.md, deploy/README-deploy.md</read_first>
  <action>
Rewrite README.md with the following structure changes. Preserve all existing content that is still accurate, but reorganize and extend it.

**Persona separation (per DOCS-02):**
- The "Live Demo" callout at the top stays but is clearly framed as "see it in action" — not the deployment path.
- Add a new top-level section "Deploy Your Own" that is the primary self-hosting path. This section must NOT reference honeyprompt.sh as a deployment target — it uses {your-domain.com} as the example domain throughout.
- The existing "Usage" section documents CLI subcommands for local development/experimentation. Keep it but position it after Deploy Your Own.

**Deploy Your Own section structure (per DOCS-01):**
The section must have four numbered subsections:

1. **Install** — Link to prebuilt binaries (existing table) or cargo install. Keep it brief, reference the existing Installation section for details.

2. **Configure** — Show `honeyprompt setup` interactive wizard flow:
   ```
   honeyprompt setup
   # Prompts for: domain, bind address, tiers, page title
   # Writes honeyprompt.toml
   ```
   Also show the zero-config alternative:
   ```
   honeyprompt serve --domain your-domain.com
   # No config file needed — generates and serves immediately
   ```

3. **Deploy** — Two paths:
   - **Quick (single binary):** Just run `honeyprompt serve --domain your-domain.com` behind your reverse proxy. Reference deploy/templates/ for Caddy/nginx config.
   - **Production (Docker):** Reference deploy/templates/docker-compose.yml and deploy/templates/Caddyfile. Show the 3-step flow: copy templates, replace {DOMAIN}, `docker compose up -d`.
   - **Systemd (bare metal):** Reference deploy/templates/honeyprompt.service. Brief instructions to install binary, copy unit file, enable service.

4. **Verify** — Show curl command to check the deployment:
   ```
   curl -I https://your-domain.com        # Should return 200
   curl https://your-domain.com/cb/v1/test  # Should return callback response
   ```

**Project Status table update:**
Add rows for phases 9-12:
- Phase 9: Server-Side Identity & Stats — Complete
- Phase 10: Landing Page — Complete
- Phase 11: Setup Wizard & Zero-Config Serve — Complete
- Phase 12: Documentation & Deploy Templates — Complete

**Other updates:**
- In the Usage section, add entries for `honeyprompt setup` and the `--domain` flag on serve (these are new v4.0 commands).
- Reference deploy/templates/ directory where relevant for advanced deployment configuration.
- Do NOT change the Ethics section, License section, or the core "What This Is" / "Proof Levels" / "How It Works" sections (content-wise — minor wording tweaks for flow are fine).
  </action>
  <verify>
    <automated>grep -c "## Deploy Your Own" README.md && grep -c "honeyprompt setup" README.md && grep -c "honeyprompt serve --domain" README.md && grep -c "deploy/templates" README.md && grep -c "honeyprompt.sh" README.md</automated>
  </verify>
  <acceptance_criteria>
    - "## Deploy Your Own" section exists with Install, Configure, Deploy, Verify subsections
    - "honeyprompt setup" appears in the Configure subsection
    - "honeyprompt serve --domain" appears in both Configure and Deploy subsections
    - deploy/templates/ is referenced in the Deploy subsection
    - honeyprompt.sh appears only in the Live Demo callout and Project Status, NOT in Deploy Your Own
    - Project Status table includes phases 9-12
  </acceptance_criteria>
  <done>README.md contains a complete Deploy Your Own guide with all four subsections, references both setup wizard and zero-config serve, links to deploy/templates/, and cleanly separates live demo from self-hosted personas</done>
</task>

</tasks>

<verification>
- `grep "## Deploy Your Own" README.md` returns a match
- `grep "honeyprompt setup" README.md` returns at least 2 matches (Configure + Usage)
- `grep "honeyprompt serve --domain" README.md` returns at least 2 matches
- `grep "deploy/templates" README.md` returns at least 1 match
- The Deploy Your Own section does NOT contain "honeyprompt.sh" (persona separation)
</verification>

<success_criteria>
A user reading only the README can follow the Deploy Your Own section to install the binary, configure their domain, deploy via Docker or systemd, and verify callbacks — without referencing any other documentation file.
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-deploy-templates/12-01-SUMMARY.md`
</output>
