---
phase: 12-documentation-deploy-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/templates/docker-compose.yml
  - deploy/templates/Caddyfile
  - deploy/templates/honeyprompt.service
autonomous: true
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "deploy/templates/ contains three template files users can copy and customize"
    - "Each template has {DOMAIN} placeholders that users replace with their actual domain"
    - "Templates are self-contained — a user can deploy with just the templates and the binary"
  artifacts:
    - path: "deploy/templates/docker-compose.yml"
      provides: "Docker Compose template for self-hosted deployment"
      contains: "{DOMAIN}"
    - path: "deploy/templates/Caddyfile"
      provides: "Caddy reverse proxy template"
      contains: "{DOMAIN}"
    - path: "deploy/templates/honeyprompt.service"
      provides: "Systemd unit template for bare metal deployment"
      contains: "honeyprompt serve"
  key_links:
    - from: "deploy/templates/docker-compose.yml"
      to: "deploy/templates/Caddyfile"
      via: "caddy service mounts Caddyfile"
      pattern: "Caddyfile:/etc/caddy/Caddyfile"
    - from: "deploy/templates/honeyprompt.service"
      to: "honeyprompt binary"
      via: "ExecStart referencing honeyprompt serve"
      pattern: "ExecStart.*honeyprompt serve"
---

<objective>
Create deploy/templates/ directory with domain-parameterized template files for Docker Compose, Caddy, and systemd deployment.

Purpose: Self-hosting users need ready-to-use deployment files they can copy, replace {DOMAIN} with their actual domain, and deploy. These are static templates — not generated by any command. They complement the README's Deploy Your Own guide (Plan 12-01).

Output: Three template files in deploy/templates/ with {DOMAIN} placeholders and inline comments explaining each section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deploy/docker-compose.yml
@deploy/Caddyfile
@deploy/honeyprompt.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/templates/ with parameterized deployment files</name>
  <files>deploy/templates/docker-compose.yml, deploy/templates/Caddyfile, deploy/templates/honeyprompt.service</files>
  <read_first>deploy/docker-compose.yml, deploy/Caddyfile, deploy/honeyprompt.service</read_first>
  <action>
Create deploy/templates/ directory and three template files. Base each on the existing deploy/ files but replace hardcoded honeyprompt.sh references with {DOMAIN} placeholders and add inline comments.

**deploy/templates/docker-compose.yml:**
Based on deploy/docker-compose.yml. Changes from the original:
- Add a header comment block explaining this is a template: "# HoneyPrompt Docker Compose Template" and "# Replace {DOMAIN} with your domain (e.g., honeyprompt.example.com)"
- Add `--domain {DOMAIN}` to the honeyprompt service command: `command: ["serve", "--domain", "{DOMAIN}"]`
- Keep the same volume structure (hp-db for SQLite persistence, Caddyfile mount, caddy-data/caddy-config)
- Keep restart: always on both services
- Add comment on the hp-db volume explaining SQLite persistence survives upgrades

**deploy/templates/Caddyfile:**
Based on deploy/Caddyfile. Changes:
- Replace `honeyprompt.sh` with `{DOMAIN}`
- Add header comment: "# HoneyPrompt Caddy Reverse Proxy Template"
- Add comment: "# Replace {DOMAIN} with your domain"
- Add comment explaining Caddy auto-provisions Let's Encrypt TLS
- Keep the reverse_proxy honeyprompt:8080 target (this is the Docker service name)

**deploy/templates/honeyprompt.service:**
Based on deploy/honeyprompt.service. Changes:
- Update Documentation URL to `https://github.com/johnzilla/honeyprompt`
- Update ExecStart to use the --domain flag: `ExecStart=/usr/local/bin/honeyprompt serve --domain {DOMAIN}`
- Remove the path argument from ExecStart (--domain uses tempdir mode or user provides --path separately)
- Add header comment: "# HoneyPrompt Systemd Service Template"
- Add comment: "# Replace {DOMAIN} with your domain"
- Add comment on KillSignal explaining why SIGINT is required (graceful shutdown of mpsc channel)
- Keep all sandbox hardening directives (NoNewPrivileges, PrivateTmp, ProtectSystem, ReadWritePaths)
- Keep WorkingDirectory=/var/lib/honeyprompt (user creates this directory)
  </action>
  <verify>
    <automated>test -f deploy/templates/docker-compose.yml && test -f deploy/templates/Caddyfile && test -f deploy/templates/honeyprompt.service && grep -c "{DOMAIN}" deploy/templates/docker-compose.yml && grep -c "{DOMAIN}" deploy/templates/Caddyfile && grep -c "{DOMAIN}" deploy/templates/honeyprompt.service</automated>
  </verify>
  <acceptance_criteria>
    - deploy/templates/docker-compose.yml exists with {DOMAIN} in command and header comment
    - deploy/templates/Caddyfile exists with {DOMAIN} replacing honeyprompt.sh and header comment
    - deploy/templates/honeyprompt.service exists with {DOMAIN} in ExecStart and header comment
    - All three files have inline comments explaining key configuration
    - docker-compose.yml includes `command: ["serve", "--domain", "{DOMAIN}"]`
    - honeyprompt.service retains KillSignal=SIGINT and sandbox hardening
  </acceptance_criteria>
  <done>Three template files exist in deploy/templates/ with {DOMAIN} placeholders, inline documentation comments, and correct structure matching the production deploy/ files</done>
</task>

</tasks>

<verification>
- `ls deploy/templates/` shows docker-compose.yml, Caddyfile, honeyprompt.service
- `grep "{DOMAIN}" deploy/templates/*` returns matches in all three files
- `grep "KillSignal=SIGINT" deploy/templates/honeyprompt.service` confirms graceful shutdown preserved
- `grep "reverse_proxy" deploy/templates/Caddyfile` confirms reverse proxy to honeyprompt:8080
- `grep "hp-db" deploy/templates/docker-compose.yml` confirms persistent volume
</verification>

<success_criteria>
A user can copy the three template files, globally replace {DOMAIN} with their actual domain, and have working deployment configs for Docker Compose + Caddy or bare-metal systemd.
</success_criteria>

<output>
After completion, create `.planning/phases/12-documentation-deploy-templates/12-02-SUMMARY.md`
</output>
